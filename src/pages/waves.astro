---
import wavesHeroImage from '../assets/waves-hero.png';
import MusicPlayer from '../components/MusicPlayer.astro';
import WavesLayout from '../layouts/WavesLayout.astro';

const pageTitle = 'waves';
const pageDescription =
	'Listen to Waves, Justin Valentini original tracks and creative audio experiments with genre variants, lyrics, playlists, and browser playback controls.';
---

<WavesLayout title={pageTitle} description={pageDescription} image={wavesHeroImage}>
	<section class="player-section">
		<MusicPlayer />
	</section>
</WavesLayout>

<style is:global>
	/* Hide scrollbar on waves page */
	html:has(.player-section) {
		overflow-y: auto;
		scrollbar-width: none;
		-ms-overflow-style: none;
	}
	
	html:has(.player-section)::-webkit-scrollbar {
		display: none;
		width: 0;
	}
</style>

<style>
	.player-section {
		margin-bottom: var(--space-3xl);
		animation: fadeInUp 0.8s ease-out both;
	}

	@keyframes fadeInUp {
		from {
			opacity: 0;
			transform: translateY(20px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}
</style>
